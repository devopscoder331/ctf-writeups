<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".Blackhol3App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Blackhol3"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Blackhol3"
            android:windowSoftInputMode="adjustResize"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name=".provider.BlobContentProvider"
            android:authorities="app.blackhol3.provider"
            android:exported="true"
            android:grantUriPermissions="true">
            <path-permission
                android:pathPattern="/ticket/*"
                android:readPermission="android.permission.READ_EXTERNAL_STORAGE" />
            <path-permission
                android:pathPattern="/pubkey/*"
                android:readPermission="android.permission.READ_EXTERNAL_STORAGE" />
        </provider>

        <service android:name=".service.MessageWebSocketBackgroundService" />

    </application>

</manifest>